__pycache__/
*.pyc

# The CLI's working directory — session leftovers, temporary files
workspace/.claude/
workspace/*.log

# Chat logs - run output, they keep growing and carry personal content.
# The user reads them from the folder (right-click in the panel -> LOG folder).
# NONE of them are in the public repo: the LOG/... references in MANTIK.md
# point to the developer's own logs.
LOG/

# Personal development notes — not for the public repo.
log_incelemesi*.txt
report.txt
caddy_gelisim.txt
caddy-atlas.html
/uzay_asansoru_*.png

# Test reports (written to a file because print is lost inside freecadcmd).
**/tests/_son_*.txt

# Addon logs
*.log

# FreeCAD
*.FCStd1
.copilot-backups/
.caddy-backups/

# Windows
Thumbs.db
desktop.ini

# Test logs: the suite now writes here instead of the real LOG/
# (see sohbet_log, CADDY_LOG_DIR). Run output, it does not belong in the repo.
tests/_gunlukler/
